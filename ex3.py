# Write a series of Python instructions that allow to declare 2 variables x and y assigning 
# them respectively the values 3 and 8.5, 
# then convert the type of these variables to a string.
# At the end, the program should display the type of these variables after conversion.

x = 3
y = 8.5
x = str(x)
y = str(y)
print(type(x))
print(type(y))